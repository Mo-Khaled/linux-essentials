Hint 2: One redirection arrow (>) creates/overwrites a file with new
content. Two arrows (>>) append without erasing what's already there. `cat`
can write a whole file's content into another file just like echo can.
