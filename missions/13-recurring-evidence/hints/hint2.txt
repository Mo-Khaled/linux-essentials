Hint 2: The wildcard for every field is *. Your line will look like:
* * * * * bash /full/path/to/scripts/cron_emitter.sh # LINUX_ESSENTIALS_CTF
`crontab -l` lists your current crontab so you can confirm it saved.
