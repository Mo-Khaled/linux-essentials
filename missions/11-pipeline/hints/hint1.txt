Hint 1: `ps -ef | grep PATTERN` filters the process list. Piping that into
`wc -l` counts the matching lines. Watch out: grep itself shows up as a
process containing your search pattern, in its own output.
